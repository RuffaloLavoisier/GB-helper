# Commit: 128854f4c686389cc20077dc54bb0eca84a24151
## Message: woodpecker: Download git-pages-cli
## Changed files:
.woodpecker/nightly.yml

## Diff:
```
diff --git a/.woodpecker/nightly.yml b/.woodpecker/nightly.yml
index 03d1c6954e..fb85079539 100644
--- a/.woodpecker/nightly.yml
+++ b/.woodpecker/nightly.yml
@@ -97,10 +97,13 @@ steps:
         from_secret: keypass
 
   push-pages-update-repo:
-    image: codeberg.org/git-pages/git-pages-cli:1.9.0
+    image: codeberg.org/freeyourgadget/android-fdroid-tools:36.0.0-androguard-4.1.2-java-21
     commands:
       - if [ -f .do_not_build ]; then return 0; fi
-      - git-pages-cli https://freeyourgadget.codeberg.page --token "$${COMMIT_TOKEN}" ../pages/pages/
+      # We can't use the git-pages-cli docker image as it does not contain sh and we can't check if we built or not
+      - curl -fsSL https://codeberg.org/git-pages/git-pages-cli/releases/download/v1.9.0/git-pages-cli.linux-amd64 -o ../git-pages-cli
+      - chmod +x ../git-pages-cli
+      - ../git-pages-cli https://freeyourgadget.codeberg.page --token "$${COMMIT_TOKEN}" ../pages/pages/
     environment:
       COMMIT_TOKEN:
         from_secret: commit_token
```
-----------------------------------
